backend:
  name: test-repo
  branch: master
media_folder: 'static/img'
public_folder: '/img'
collections:
  - name: 'test'
    label: 'test'
    folder: 'src/lib/data/test'
    create: true
    slug: '{{slug}}'
    fields:
      - { label: 'Title', name: 'title', widget: 'string' }
